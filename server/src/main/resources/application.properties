## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)

# Localhost Environment
spring.datasource.url = jdbc:mysql://localhost:3306/test?allowPublicKeyRetrieval=true&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=false
spring.datasource.username = wind
spring.datasource.password = 12345678

# Cloud Environment
#spring.datasource.url = jdbc:mysql://aa1g4ad7imhj84g.cnjcnhbwv6qn.ap-southeast-1.rds.amazonaws.com:3306/test?allowPublicKeyRetrieval=true&autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true&useSSL=false
#spring.datasource.username = tienthai
#spring.datasource.password = 12345678

## Hibernate Properties
spring.jpa.show-sql=true

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# Listen on the port passed through `PORT` environment variable (Fallback to 8080)
server.port=5000
